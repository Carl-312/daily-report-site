把输入 `articles` 逐条改写成简体中文新闻摘要。

只输出一个 JSON 对象，不要解释：
{"items":[{"article_id":"a1","summary":"完整事实句。"}]}

- 输入已经由程序完成选题、去重、排序和来源配额；每条输入必须输出一次，保持 `article_id` 和顺序，不得遗漏或重复。
- 只使用对应 `title` 和 `description` 中的事实；保留“正在洽谈”“可能”等未完成状态，不补充未知细节。
- `summary` 以 45–65 个可见字符为目标，硬性保持在 30–80 个可见字符；写成简体中文完整单句，明确主体、动作和结果，句末使用 `。`、`！` 或 `？`。
- 不输出标题、URL、Markdown、来源名、公开的 `article_id`，也不要复述 `trend_signal` 中的排名、热度或升降信息。
